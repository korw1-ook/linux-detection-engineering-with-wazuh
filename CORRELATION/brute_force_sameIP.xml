##Attackers aren’t stupid they stretch attempts so SIEM ignores them. 
SO WE WILL IMPLEMENT SLOW BRUTEFORCE DETECTION 

#20 ssh brute force detection in 1 hour

<rule id="120010" level="10" frequency="20" timeframe="3600">
  <if_sid>5716</if_sid>
  <match>Failed password for</match>
  <same_source_ip>true</same_source_ip>
  <description>Slow brute force: same IP failed 20 SSH logins in 1 hour</description>
  <group>sshd,bruteforce,linux</group>
</rule>
